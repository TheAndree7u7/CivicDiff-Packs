# CivicDiff Packs - Environment Variables
# Copy this file to .env.local and fill in the values

# Gemini API Key (required for Live Mode only; Demo Mode works without it)
GEMINI_API_KEY=

# Gemini Model (optional, defaults to gemini-2.0-flash)
# Options: gemini-2.0-flash, gemini-2.5-pro-preview-06-05
GEMINI_MODEL=gemini-2.0-flash

# Thinking Level for Gemini (optional, defaults to "medium")
# Options: medium, high
THINKING_LEVEL=medium

# ── Rate Limiting (protects your free API key) ───────────────
# Max live API calls per minute (default: 5)
RATE_LIMIT_LIVE_RPM=5

# Max demo calls per minute (default: 20)
RATE_LIMIT_DEMO_RPM=20

# Max live API calls per hour — budget cap (default: 50)
MAX_LIVE_CALLS_PER_HOUR=50
